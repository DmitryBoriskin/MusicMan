@{
    Layout = null;
}

<div class="WelcomeContentLeft">
    <div class="LoginCaption">@Model.Title.ToString() @Model.HttpCode.ToString()</div>
</div>
<div class="WelcomeContentRight">
    @Html.Raw(Model.Message)
</div>
